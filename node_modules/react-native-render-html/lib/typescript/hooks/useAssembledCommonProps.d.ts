import { TNode } from '@native-html/transient-render-engine';
import { CustomRenderer, CustomRendererProps, TDefaultRenderer, TDefaultRendererProps } from '../shared-types';
import { TNodeSubRendererProps } from '../internal-types';
/**
 * @internal
 */
export default function useAssembledCommonProps<T extends TNode>({ tnode, propsFromParent, sharedProps }: TNodeSubRendererProps<T>, TDefault: TDefaultRenderer<T>): {
    assembledProps: CustomRendererProps<T> & TDefaultRendererProps<T>;
    Renderer: CustomRenderer<T>;
};
