import { PropsWithChildren, ReactElement } from 'react';
import { ViewStyle } from 'react-native';
import { IMGElementProps } from './img-types';
/**
 * Container for the {@link IMGElement} component.
 */
export default function IMGElementContainer({ style, onPress, testID, children }: PropsWithChildren<Pick<IMGElementProps, 'onPress' | 'testID'> & {
    style: ViewStyle;
}>): ReactElement;
